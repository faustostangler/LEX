"""PostgreSQL 16 Implementation of the Gazette and Normative Acts Repository Port.

Uses SQLAlchemy 2.0 ORM and native PostgreSQL dialect features to persist GazetteEdition and
NormativeAct discrete legal entities with idempotent ON CONFLICT resolution and TOAST compression.
"""

import uuid
from datetime import UTC, datetime

from sqlalchemy import select
from sqlalchemy.dialects.postgresql import insert as pg_insert
from sqlalchemy.orm import Session

from lex.ingestion.application.ports import GazetteRepositoryPort
from lex.ingestion.domain.entities import GazetteEdition, NormativeAct
from lex.ingestion.domain.value_objects import (
    ClassificationSource,
    DocumentHash,
    FederativeTier,
    GazetteDate,
    IngestionStatus,
    TerritoryId,
)
from lex.ingestion.infrastructure.persistence.models import (
    GazetteEditionModel,
    NormativeActModel,
)
from lex.shared_kernel.value_objects import (
    HierarchicalGroup,
    PublicationNature,
)


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁPostgresGazetteRepositoryǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPostgresGazetteRepositoryǁsave_normative_act__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut: MutantDict = {}  # type: ignore


class PostgresGazetteRepository(GazetteRepositoryPort):
    """Hexagonal Adapter fulfilling GazetteRepositoryPort via PostgreSQL 16."""

    @_mutmut_mutated(mutants_xǁPostgresGazetteRepositoryǁ__init____mutmut)
    def __init__(self, session: Session) -> None:
        self._session = session

    def xǁPostgresGazetteRepositoryǁ__init____mutmut_orig(self, session: Session) -> None:
        self._session = session

    def xǁPostgresGazetteRepositoryǁ__init____mutmut_1(self, session: Session) -> None:
        self._session = None

    @_mutmut_mutated(mutants_xǁPostgresGazetteRepositoryǁsave__mutmut)
    def save(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_orig(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_1(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = None
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_2(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = None

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_3(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None or bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_4(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_5(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name != "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_6(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "XXpostgresqlXX"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_7(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "POSTGRESQL"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_8(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = None
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_9(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=None,
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_10(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=None,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_11(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=None,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_12(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=None,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_13(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=None,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_14(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=None,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_15(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=None,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_16(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=None,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_17(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=None,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_18(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=None,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_19(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=None,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_20(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=None,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_21(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=None,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_22(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_23(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_24(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_25(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_26(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_27(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_28(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_29(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_30(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_31(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_32(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_33(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_34(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_35(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(None).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_36(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id and uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_37(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = None
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_38(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(None)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_39(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint=None,
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_40(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_=None,
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_41(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_42(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_43(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="XXuq_gazette_edition_natural_keyXX",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_44(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="UQ_GAZETTE_EDITION_NATURAL_KEY",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_45(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "XXsummary_sha256XX": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_46(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "SUMMARY_SHA256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_47(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "XXtotal_actsXX": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_48(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "TOTAL_ACTS": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_49(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "XXingestion_statusXX": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_50(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "INGESTION_STATUS": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_51(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "XXsource_urlXX": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_52(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "SOURCE_URL": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_53(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "XXupdated_atXX": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_54(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "UPDATED_AT": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_55(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(None),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_56(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = None
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_57(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(None).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_58(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = None

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_59(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    None
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_60(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        None,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_61(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        None,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_62(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        None,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_63(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        None,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_64(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        None,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_65(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_66(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_67(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_68(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_69(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_70(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(None).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_71(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id != edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_72(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date != edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_73(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number != edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_74(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section != edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_75(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition != edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_76(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_77(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = None
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_78(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = None
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_79(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = None
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_80(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = None
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_81(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = None
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_82(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(None)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_83(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = None
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_84(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = None
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_85(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id and uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_86(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = None
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_87(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=None,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_88(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=None,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_89(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=None,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_90(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=None,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_91(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=None,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_92(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=None,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_93(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=None,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_94(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=None,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_95(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=None,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_96(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=None,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_97(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=None,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_98(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=None,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_99(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=None,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_100(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_101(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_102(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_103(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_104(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_105(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_106(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_107(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_108(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_109(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_110(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_111(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_112(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_113(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(None)

            self._session.commit()
            return edition.model_copy(update={"id": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_114(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update=None)
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_115(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"XXidXX": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave__mutmut_116(self, edition: GazetteEdition) -> GazetteEdition:
        """Persist a GazetteEdition container record with idempotent upsert and return entity."""
        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            persisted_id: uuid.UUID
            if is_postgres:
                stmt = pg_insert(GazetteEditionModel).values(
                    id=edition.id or uuid.uuid4(),
                    territory_id=edition.territory_id.code,
                    tier=edition.tier.value,
                    date=edition.date.value,
                    edition_number=edition.edition_number,
                    section=edition.section,
                    is_extra_edition=edition.is_extra_edition,
                    power=edition.power,
                    source_url=edition.source_url,
                    summary_sha256=edition.summary_hash.hex_digest,
                    total_acts=edition.total_acts,
                    ingestion_status=edition.ingestion_status.value,
                    scraped_at=edition.scraped_at,
                )
                upsert_stmt = stmt.on_conflict_do_update(
                    constraint="uq_gazette_edition_natural_key",
                    set_={
                        "summary_sha256": stmt.excluded.summary_sha256,
                        "total_acts": stmt.excluded.total_acts,
                        "ingestion_status": stmt.excluded.ingestion_status,
                        "source_url": stmt.excluded.source_url,
                        "updated_at": datetime.now(UTC),
                    },
                ).returning(GazetteEditionModel.id)
                persisted_id = self._session.execute(upsert_stmt).scalar_one()
            else:
                existing = self._session.execute(
                    select(GazetteEditionModel).where(
                        GazetteEditionModel.territory_id == edition.territory_id.code,
                        GazetteEditionModel.date == edition.date.value,
                        GazetteEditionModel.edition_number == edition.edition_number,
                        GazetteEditionModel.section == edition.section,
                        GazetteEditionModel.is_extra_edition == edition.is_extra_edition,
                    )
                ).scalar_one_or_none()

                if existing is not None:
                    existing.summary_sha256 = edition.summary_hash.hex_digest
                    existing.total_acts = edition.total_acts
                    existing.ingestion_status = edition.ingestion_status.value
                    existing.source_url = edition.source_url
                    existing.updated_at = datetime.now(UTC)
                    persisted_id = existing.id
                else:
                    persisted_id = edition.id or uuid.uuid4()
                    model = GazetteEditionModel(
                        id=persisted_id,
                        territory_id=edition.territory_id.code,
                        tier=edition.tier.value,
                        date=edition.date.value,
                        edition_number=edition.edition_number,
                        section=edition.section,
                        is_extra_edition=edition.is_extra_edition,
                        power=edition.power,
                        source_url=edition.source_url,
                        summary_sha256=edition.summary_hash.hex_digest,
                        total_acts=edition.total_acts,
                        ingestion_status=edition.ingestion_status.value,
                        scraped_at=edition.scraped_at,
                    )
                    self._session.add(model)

            self._session.commit()
            return edition.model_copy(update={"ID": persisted_id})
        except Exception:
            self._session.rollback()
            raise

    @_mutmut_mutated(mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut)
    def get_by_territory_and_date(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_orig(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_1(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = None
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_2(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            None,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_3(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            None,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_4(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_5(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_6(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(None).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_7(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id != territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_8(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date != date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_9(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_10(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = None

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_11(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(None)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_12(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section != section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_13(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = None
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_14(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(None).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_15(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is not None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_16(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=None,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_17(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=None,
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_18(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=None,
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_19(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=None,
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_20(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=None,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_21(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=None,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_22(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=None,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_23(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=None,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_24(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=None,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_25(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=None,
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_26(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=None,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_27(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=None,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_28(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=None,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_29(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_30(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_31(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_32(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_33(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_34(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_35(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_36(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_37(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_38(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_39(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_40(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_41(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_42(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(None),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_43(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(None),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_44(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(None),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_45(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(None),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(model.ingestion_status),
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_46(
        self,
        territory_id: TerritoryId,
        date: GazetteDate,
        section: str | None = None,
    ) -> GazetteEdition | None:
        """Retrieve a gazette edition container by territory, date and section."""
        stmt = select(GazetteEditionModel).where(
            GazetteEditionModel.territory_id == territory_id.code,
            GazetteEditionModel.date == date.value,
        )
        if section is not None:
            stmt = stmt.where(GazetteEditionModel.section == section)

        model = self._session.execute(stmt).scalars().first()
        if model is None:
            return None

        return GazetteEdition(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            tier=FederativeTier(model.tier),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            power=model.power,
            source_url=model.source_url,
            summary_hash=DocumentHash.from_hex(model.summary_sha256),
            total_acts=model.total_acts,
            ingestion_status=IngestionStatus(None),
            scraped_at=model.scraped_at,
        )

    @_mutmut_mutated(mutants_xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut)
    def exists_by_hash(self, summary_hash: DocumentHash) -> bool:
        """Check if an edition container already exists matching summary_hash."""
        stmt = select(GazetteEditionModel.id).where(
            GazetteEditionModel.summary_sha256 == summary_hash.hex_digest
        )
        result = self._session.execute(stmt).first()
        return result is not None

    def xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut_orig(self, summary_hash: DocumentHash) -> bool:
        """Check if an edition container already exists matching summary_hash."""
        stmt = select(GazetteEditionModel.id).where(
            GazetteEditionModel.summary_sha256 == summary_hash.hex_digest
        )
        result = self._session.execute(stmt).first()
        return result is not None

    def xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut_1(self, summary_hash: DocumentHash) -> bool:
        """Check if an edition container already exists matching summary_hash."""
        stmt = None
        result = self._session.execute(stmt).first()
        return result is not None

    def xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut_2(self, summary_hash: DocumentHash) -> bool:
        """Check if an edition container already exists matching summary_hash."""
        stmt = select(GazetteEditionModel.id).where(
            None
        )
        result = self._session.execute(stmt).first()
        return result is not None

    def xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut_3(self, summary_hash: DocumentHash) -> bool:
        """Check if an edition container already exists matching summary_hash."""
        stmt = select(None).where(
            GazetteEditionModel.summary_sha256 == summary_hash.hex_digest
        )
        result = self._session.execute(stmt).first()
        return result is not None

    def xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut_4(self, summary_hash: DocumentHash) -> bool:
        """Check if an edition container already exists matching summary_hash."""
        stmt = select(GazetteEditionModel.id).where(
            GazetteEditionModel.summary_sha256 != summary_hash.hex_digest
        )
        result = self._session.execute(stmt).first()
        return result is not None

    def xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut_5(self, summary_hash: DocumentHash) -> bool:
        """Check if an edition container already exists matching summary_hash."""
        stmt = select(GazetteEditionModel.id).where(
            GazetteEditionModel.summary_sha256 == summary_hash.hex_digest
        )
        result = None
        return result is not None

    def xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut_6(self, summary_hash: DocumentHash) -> bool:
        """Check if an edition container already exists matching summary_hash."""
        stmt = select(GazetteEditionModel.id).where(
            GazetteEditionModel.summary_sha256 == summary_hash.hex_digest
        )
        result = self._session.execute(None).first()
        return result is not None

    def xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut_7(self, summary_hash: DocumentHash) -> bool:
        """Check if an edition container already exists matching summary_hash."""
        stmt = select(GazetteEditionModel.id).where(
            GazetteEditionModel.summary_sha256 == summary_hash.hex_digest
        )
        result = self._session.execute(stmt).first()
        return result is None

    @_mutmut_mutated(mutants_xǁPostgresGazetteRepositoryǁsave_normative_act__mutmut)
    def save_normative_act(self, act: NormativeAct) -> None:
        """Persist a discrete normative act with idempotent ON CONFLICT semantics."""
        self.save_normative_acts_bulk([act])

    def xǁPostgresGazetteRepositoryǁsave_normative_act__mutmut_orig(self, act: NormativeAct) -> None:
        """Persist a discrete normative act with idempotent ON CONFLICT semantics."""
        self.save_normative_acts_bulk([act])

    def xǁPostgresGazetteRepositoryǁsave_normative_act__mutmut_1(self, act: NormativeAct) -> None:
        """Persist a discrete normative act with idempotent ON CONFLICT semantics."""
        self.save_normative_acts_bulk(None)

    @_mutmut_mutated(mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut)
    def save_normative_acts_bulk(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_orig(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_1(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_2(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = None
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_3(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = None

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_4(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None or bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_5(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_6(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name != "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_7(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "XXpostgresqlXX"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_8(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "POSTGRESQL"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_9(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = None
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_10(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=None,
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_11(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=None,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_12(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=None,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_13(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=None,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_14(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=None,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_15(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=None,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_16(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=None,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_17(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=None,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_18(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=None,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_19(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=None,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_20(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=None,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_21(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=None,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_22(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=None,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_23(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=None,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_24(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=None,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_25(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=None,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_26(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=None,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_27(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=None,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_28(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=None,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_29(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=None,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_30(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=None,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_31(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=None,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_32(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=None,
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_33(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=None,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_34(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=None,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_35(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=None,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_36(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=None,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_37(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=None,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_38(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=None,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_39(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_40(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_41(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_42(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_43(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_44(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_45(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_46(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_47(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_48(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_49(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_50(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_51(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_52(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_53(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_54(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_55(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_56(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_57(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_58(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_59(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_60(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_61(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_62(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_63(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_64(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_65(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_66(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_67(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_68(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(None).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_69(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id and uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_70(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(None),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_71(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = None
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_72(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint=None,
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_73(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_=None,
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_74(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_75(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_76(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="XXuq_normative_act_natural_keyXX",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_77(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="UQ_NORMATIVE_ACT_NATURAL_KEY",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_78(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "XXtitleXX": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_79(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "TITLE": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_80(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "XXementaXX": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_81(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "EMENTA": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_82(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "XXhierarchyXX": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_83(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "HIERARCHY": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_84(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "XXauthority_nameXX": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_85(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "AUTHORITY_NAME": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_86(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "XXauthority_roleXX": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_87(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "AUTHORITY_ROLE": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_88(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "XXraw_contentXX": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_89(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "RAW_CONTENT": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_90(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "XXchar_countXX": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_91(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "CHAR_COUNT": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_92(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "XXhierarchical_groupXX": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_93(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "HIERARCHICAL_GROUP": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_94(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "XXhierarchical_rankXX": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_95(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "HIERARCHICAL_RANK": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_96(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "XXpublication_natureXX": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_97(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "PUBLICATION_NATURE": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_98(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "XXcanonical_urnXX": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_99(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "CANONICAL_URN": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_100(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "XXis_stubXX": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_101(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "IS_STUB": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_102(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "XXupdated_atXX": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_103(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "UPDATED_AT": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_104(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(None),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_105(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(None)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_106(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = None

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_107(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        None
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_108(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            None,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_109(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            None,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_110(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            None,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_111(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_112(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_113(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_114(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(None).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_115(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id != act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_116(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url != act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_117(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 != act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_118(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_119(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = None
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_120(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = None
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_121(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = None
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_122(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = None
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_123(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = None
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_124(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = None
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_125(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = None
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_126(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = None
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_127(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(None)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_128(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = None
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_129(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = None
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_130(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = None
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_131(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = None
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_132(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = None
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_133(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(None)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_134(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = None
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_135(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=None,
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_136(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=None,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_137(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=None,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_138(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=None,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_139(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=None,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_140(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=None,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_141(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=None,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_142(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=None,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_143(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=None,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_144(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=None,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_145(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=None,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_146(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=None,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_147(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=None,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_148(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=None,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_149(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=None,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_150(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=None,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_151(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=None,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_152(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=None,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_153(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=None,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_154(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=None,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_155(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=None,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_156(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=None,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_157(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=None,
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_158(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=None,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_159(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=None,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_160(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=None,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_161(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=None,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_162(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=None,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_163(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=None,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_164(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_165(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_166(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_167(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_168(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_169(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_170(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_171(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_172(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_173(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_174(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_175(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_176(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_177(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_178(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_179(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_180(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_181(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_182(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_183(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_184(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_185(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_186(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_187(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_188(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_189(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_190(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_191(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_192(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_193(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id and uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_194(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(None),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(model)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    def xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_195(self, acts: list[NormativeAct]) -> None:
        """Persist a batch of discrete normative acts with bulk upsert and rollback safety."""
        if not acts:
            return

        bind = self._session.get_bind()
        is_postgres = bind is not None and bind.dialect.name == "postgresql"

        try:
            if is_postgres:
                for act in acts:
                    stmt = pg_insert(NormativeActModel).values(
                        id=act.id or uuid.uuid4(),
                        edition_id=act.edition_id,
                        territory_id=act.territory_id.code,
                        date=act.date.value,
                        section=act.section,
                        edition_number=act.edition_number,
                        is_extra_edition=act.is_extra_edition,
                        act_type=act.act_type,
                        act_number=act.act_number,
                        act_year=act.act_year,
                        title=act.title,
                        ementa=act.ementa,
                        hierarchy=act.hierarchy,
                        authority_name=act.authority_name,
                        authority_role=act.authority_role,
                        source_url=act.source_url,
                        content_sha256=act.content_hash.hex_digest,
                        char_count=act.char_count,
                        raw_content=act.raw_content,
                        structured_content=act.structured_content,
                        classification_source=act.classification_source.value,
                        classification_confidence=act.classification_confidence,
                        hierarchical_group=int(act.hierarchical_group),
                        hierarchical_rank=act.hierarchical_rank,
                        publication_nature=act.publication_nature.value,
                        canonical_urn=act.canonical_urn,
                        is_stub=act.is_stub,
                        metadata_json=act.metadata_json,
                        scraped_at=act.scraped_at,
                    )
                    upsert_stmt = stmt.on_conflict_do_update(
                        constraint="uq_normative_act_natural_key",
                        set_={
                            "title": stmt.excluded.title,
                            "ementa": stmt.excluded.ementa,
                            "hierarchy": stmt.excluded.hierarchy,
                            "authority_name": stmt.excluded.authority_name,
                            "authority_role": stmt.excluded.authority_role,
                            "raw_content": stmt.excluded.raw_content,
                            "char_count": stmt.excluded.char_count,
                            "hierarchical_group": stmt.excluded.hierarchical_group,
                            "hierarchical_rank": stmt.excluded.hierarchical_rank,
                            "publication_nature": stmt.excluded.publication_nature,
                            "canonical_urn": stmt.excluded.canonical_urn,
                            "is_stub": stmt.excluded.is_stub,
                            "updated_at": datetime.now(UTC),
                        },
                    )
                    self._session.execute(upsert_stmt)
            else:
                for act in acts:
                    existing = self._session.execute(
                        select(NormativeActModel).where(
                            NormativeActModel.edition_id == act.edition_id,
                            NormativeActModel.source_url == act.source_url,
                            NormativeActModel.content_sha256 == act.content_hash.hex_digest,
                        )
                    ).scalar_one_or_none()

                    if existing is not None:
                        existing.title = act.title
                        existing.ementa = act.ementa
                        existing.hierarchy = act.hierarchy
                        existing.authority_name = act.authority_name
                        existing.authority_role = act.authority_role
                        existing.raw_content = act.raw_content
                        existing.char_count = act.char_count
                        existing.hierarchical_group = int(act.hierarchical_group)
                        existing.hierarchical_rank = act.hierarchical_rank
                        existing.publication_nature = act.publication_nature.value
                        existing.canonical_urn = act.canonical_urn
                        existing.is_stub = act.is_stub
                        existing.updated_at = datetime.now(UTC)
                    else:
                        model = NormativeActModel(
                            id=act.id or uuid.uuid4(),
                            edition_id=act.edition_id,
                            territory_id=act.territory_id.code,
                            date=act.date.value,
                            section=act.section,
                            edition_number=act.edition_number,
                            is_extra_edition=act.is_extra_edition,
                            act_type=act.act_type,
                            act_number=act.act_number,
                            act_year=act.act_year,
                            title=act.title,
                            ementa=act.ementa,
                            hierarchy=act.hierarchy,
                            authority_name=act.authority_name,
                            authority_role=act.authority_role,
                            source_url=act.source_url,
                            content_sha256=act.content_hash.hex_digest,
                            char_count=act.char_count,
                            raw_content=act.raw_content,
                            structured_content=act.structured_content,
                            classification_source=act.classification_source.value,
                            classification_confidence=act.classification_confidence,
                            hierarchical_group=int(act.hierarchical_group),
                            hierarchical_rank=act.hierarchical_rank,
                            publication_nature=act.publication_nature.value,
                            canonical_urn=act.canonical_urn,
                            is_stub=act.is_stub,
                            metadata_json=act.metadata_json,
                            scraped_at=act.scraped_at,
                        )
                        self._session.add(None)

            self._session.commit()
        except Exception:
            self._session.rollback()
            raise

    @_mutmut_mutated(mutants_xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut)
    def get_act_by_id(self, act_id: uuid.UUID) -> NormativeAct | None:
        """Retrieve an individual normative act by primary key."""
        model = self._session.execute(
            select(NormativeActModel).where(NormativeActModel.id == act_id)
        ).scalar_one_or_none()
        if model is None:
            return None
        return self._map_act_model_to_domain(model)

    def xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut_orig(self, act_id: uuid.UUID) -> NormativeAct | None:
        """Retrieve an individual normative act by primary key."""
        model = self._session.execute(
            select(NormativeActModel).where(NormativeActModel.id == act_id)
        ).scalar_one_or_none()
        if model is None:
            return None
        return self._map_act_model_to_domain(model)

    def xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut_1(self, act_id: uuid.UUID) -> NormativeAct | None:
        """Retrieve an individual normative act by primary key."""
        model = None
        if model is None:
            return None
        return self._map_act_model_to_domain(model)

    def xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut_2(self, act_id: uuid.UUID) -> NormativeAct | None:
        """Retrieve an individual normative act by primary key."""
        model = self._session.execute(
            None
        ).scalar_one_or_none()
        if model is None:
            return None
        return self._map_act_model_to_domain(model)

    def xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut_3(self, act_id: uuid.UUID) -> NormativeAct | None:
        """Retrieve an individual normative act by primary key."""
        model = self._session.execute(
            select(NormativeActModel).where(None)
        ).scalar_one_or_none()
        if model is None:
            return None
        return self._map_act_model_to_domain(model)

    def xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut_4(self, act_id: uuid.UUID) -> NormativeAct | None:
        """Retrieve an individual normative act by primary key."""
        model = self._session.execute(
            select(None).where(NormativeActModel.id == act_id)
        ).scalar_one_or_none()
        if model is None:
            return None
        return self._map_act_model_to_domain(model)

    def xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut_5(self, act_id: uuid.UUID) -> NormativeAct | None:
        """Retrieve an individual normative act by primary key."""
        model = self._session.execute(
            select(NormativeActModel).where(NormativeActModel.id != act_id)
        ).scalar_one_or_none()
        if model is None:
            return None
        return self._map_act_model_to_domain(model)

    def xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut_6(self, act_id: uuid.UUID) -> NormativeAct | None:
        """Retrieve an individual normative act by primary key."""
        model = self._session.execute(
            select(NormativeActModel).where(NormativeActModel.id == act_id)
        ).scalar_one_or_none()
        if model is not None:
            return None
        return self._map_act_model_to_domain(model)

    def xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut_7(self, act_id: uuid.UUID) -> NormativeAct | None:
        """Retrieve an individual normative act by primary key."""
        model = self._session.execute(
            select(NormativeActModel).where(NormativeActModel.id == act_id)
        ).scalar_one_or_none()
        if model is None:
            return None
        return self._map_act_model_to_domain(None)

    @_mutmut_mutated(mutants_xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut)
    def find_acts_by_edition(self, edition_id: uuid.UUID) -> list[NormativeAct]:
        """Retrieve all normative acts associated with a specific gazette edition."""
        models = (
            self._session.execute(
                select(NormativeActModel).where(NormativeActModel.edition_id == edition_id)
            )
            .scalars()
            .all()
        )
        return [self._map_act_model_to_domain(m) for m in models]

    def xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut_orig(self, edition_id: uuid.UUID) -> list[NormativeAct]:
        """Retrieve all normative acts associated with a specific gazette edition."""
        models = (
            self._session.execute(
                select(NormativeActModel).where(NormativeActModel.edition_id == edition_id)
            )
            .scalars()
            .all()
        )
        return [self._map_act_model_to_domain(m) for m in models]

    def xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut_1(self, edition_id: uuid.UUID) -> list[NormativeAct]:
        """Retrieve all normative acts associated with a specific gazette edition."""
        models = None
        return [self._map_act_model_to_domain(m) for m in models]

    def xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut_2(self, edition_id: uuid.UUID) -> list[NormativeAct]:
        """Retrieve all normative acts associated with a specific gazette edition."""
        models = (
            self._session.execute(
                None
            )
            .scalars()
            .all()
        )
        return [self._map_act_model_to_domain(m) for m in models]

    def xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut_3(self, edition_id: uuid.UUID) -> list[NormativeAct]:
        """Retrieve all normative acts associated with a specific gazette edition."""
        models = (
            self._session.execute(
                select(NormativeActModel).where(None)
            )
            .scalars()
            .all()
        )
        return [self._map_act_model_to_domain(m) for m in models]

    def xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut_4(self, edition_id: uuid.UUID) -> list[NormativeAct]:
        """Retrieve all normative acts associated with a specific gazette edition."""
        models = (
            self._session.execute(
                select(None).where(NormativeActModel.edition_id == edition_id)
            )
            .scalars()
            .all()
        )
        return [self._map_act_model_to_domain(m) for m in models]

    def xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut_5(self, edition_id: uuid.UUID) -> list[NormativeAct]:
        """Retrieve all normative acts associated with a specific gazette edition."""
        models = (
            self._session.execute(
                select(NormativeActModel).where(NormativeActModel.edition_id != edition_id)
            )
            .scalars()
            .all()
        )
        return [self._map_act_model_to_domain(m) for m in models]

    def xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut_6(self, edition_id: uuid.UUID) -> list[NormativeAct]:
        """Retrieve all normative acts associated with a specific gazette edition."""
        models = (
            self._session.execute(
                select(NormativeActModel).where(NormativeActModel.edition_id == edition_id)
            )
            .scalars()
            .all()
        )
        return [self._map_act_model_to_domain(None) for m in models]

    @_mutmut_mutated(mutants_xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut)
    def exists_act_by_hash(self, content_hash: DocumentHash) -> bool:
        """Check if an act already exists with the given content SHA-256."""
        stmt = select(NormativeActModel.id).where(
            NormativeActModel.content_sha256 == content_hash.hex_digest
        )
        return self._session.execute(stmt).first() is not None

    def xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut_orig(self, content_hash: DocumentHash) -> bool:
        """Check if an act already exists with the given content SHA-256."""
        stmt = select(NormativeActModel.id).where(
            NormativeActModel.content_sha256 == content_hash.hex_digest
        )
        return self._session.execute(stmt).first() is not None

    def xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut_1(self, content_hash: DocumentHash) -> bool:
        """Check if an act already exists with the given content SHA-256."""
        stmt = None
        return self._session.execute(stmt).first() is not None

    def xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut_2(self, content_hash: DocumentHash) -> bool:
        """Check if an act already exists with the given content SHA-256."""
        stmt = select(NormativeActModel.id).where(
            None
        )
        return self._session.execute(stmt).first() is not None

    def xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut_3(self, content_hash: DocumentHash) -> bool:
        """Check if an act already exists with the given content SHA-256."""
        stmt = select(None).where(
            NormativeActModel.content_sha256 == content_hash.hex_digest
        )
        return self._session.execute(stmt).first() is not None

    def xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut_4(self, content_hash: DocumentHash) -> bool:
        """Check if an act already exists with the given content SHA-256."""
        stmt = select(NormativeActModel.id).where(
            NormativeActModel.content_sha256 != content_hash.hex_digest
        )
        return self._session.execute(stmt).first() is not None

    def xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut_5(self, content_hash: DocumentHash) -> bool:
        """Check if an act already exists with the given content SHA-256."""
        stmt = select(NormativeActModel.id).where(
            NormativeActModel.content_sha256 == content_hash.hex_digest
        )
        return self._session.execute(None).first() is not None

    def xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut_6(self, content_hash: DocumentHash) -> bool:
        """Check if an act already exists with the given content SHA-256."""
        stmt = select(NormativeActModel.id).where(
            NormativeActModel.content_sha256 == content_hash.hex_digest
        )
        return self._session.execute(stmt).first() is None

    @_mutmut_mutated(mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut)
    def _map_act_model_to_domain(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_orig(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_1(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=None,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_2(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=None,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_3(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=None,
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_4(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=None,
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_5(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=None,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_6(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=None,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_7(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=None,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_8(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=None,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_9(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=None,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_10(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=None,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_11(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=None,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_12(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=None,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_13(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=None,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_14(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=None,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_15(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=None,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_16(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=None,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_17(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=None,
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_18(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=None,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_19(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=None,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_20(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=None,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_21(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=None,
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_22(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=None,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_23(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=None,
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_24(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=None,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_25(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=None,
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_26(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=None,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_27(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=None,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_28(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=None,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_29(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=None,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_30(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_31(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_32(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_33(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_34(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_35(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_36(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_37(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_38(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_39(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_40(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_41(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_42(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_43(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_44(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_45(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_46(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_47(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_48(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_49(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_50(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_51(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_52(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_53(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_54(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_55(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_56(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_57(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_58(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_59(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(None),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_60(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(None),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_61(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(None),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_62(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(None),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_63(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(None),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(model.publication_nature),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

    def xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_64(self, model: NormativeActModel) -> NormativeAct:
        """Map ORM model to pure domain entity."""
        return NormativeAct(
            id=model.id,
            edition_id=model.edition_id,
            territory_id=TerritoryId.from_code(model.territory_id),
            date=GazetteDate.from_date(model.date),
            section=model.section,
            edition_number=model.edition_number,
            is_extra_edition=model.is_extra_edition,
            act_type=model.act_type,
            act_number=model.act_number,
            act_year=model.act_year,
            title=model.title,
            ementa=model.ementa,
            hierarchy=model.hierarchy,
            authority_name=model.authority_name,
            authority_role=model.authority_role,
            source_url=model.source_url,
            content_hash=DocumentHash.from_hex(model.content_sha256),
            char_count=model.char_count,
            raw_content=model.raw_content,
            structured_content=model.structured_content,
            classification_source=ClassificationSource(model.classification_source),
            classification_confidence=model.classification_confidence,
            hierarchical_group=HierarchicalGroup(model.hierarchical_group),
            hierarchical_rank=model.hierarchical_rank,
            publication_nature=PublicationNature(None),
            canonical_urn=model.canonical_urn,
            is_stub=model.is_stub,
            metadata_json=model.metadata_json,
            scraped_at=model.scraped_at,
        )

mutants_xǁPostgresGazetteRepositoryǁ__init____mutmut['_mutmut_orig'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ__init____mutmut['xǁPostgresGazetteRepositoryǁ__init____mutmut_1'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ__init____mutmut_1 # type: ignore # mutmut generated

mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['_mutmut_orig'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_orig # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_1'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_1 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_2'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_2 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_3'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_3 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_4'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_4 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_5'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_5 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_6'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_6 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_7'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_7 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_8'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_8 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_9'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_9 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_10'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_10 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_11'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_11 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_12'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_12 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_13'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_13 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_14'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_14 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_15'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_15 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_16'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_16 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_17'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_17 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_18'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_18 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_19'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_19 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_20'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_20 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_21'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_21 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_22'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_22 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_23'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_23 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_24'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_24 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_25'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_25 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_26'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_26 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_27'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_27 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_28'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_28 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_29'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_29 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_30'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_30 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_31'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_31 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_32'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_32 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_33'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_33 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_34'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_34 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_35'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_35 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_36'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_36 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_37'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_37 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_38'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_38 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_39'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_39 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_40'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_40 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_41'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_41 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_42'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_42 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_43'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_43 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_44'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_44 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_45'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_45 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_46'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_46 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_47'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_47 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_48'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_48 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_49'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_49 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_50'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_50 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_51'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_51 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_52'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_52 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_53'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_53 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_54'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_54 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_55'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_55 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_56'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_56 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_57'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_57 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_58'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_58 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_59'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_59 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_60'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_60 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_61'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_61 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_62'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_62 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_63'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_63 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_64'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_64 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_65'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_65 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_66'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_66 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_67'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_67 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_68'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_68 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_69'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_69 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_70'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_70 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_71'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_71 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_72'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_72 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_73'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_73 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_74'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_74 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_75'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_75 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_76'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_76 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_77'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_77 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_78'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_78 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_79'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_79 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_80'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_80 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_81'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_81 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_82'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_82 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_83'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_83 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_84'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_84 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_85'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_85 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_86'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_86 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_87'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_87 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_88'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_88 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_89'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_89 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_90'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_90 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_91'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_91 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_92'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_92 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_93'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_93 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_94'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_94 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_95'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_95 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_96'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_96 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_97'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_97 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_98'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_98 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_99'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_99 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_100'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_100 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_101'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_101 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_102'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_102 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_103'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_103 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_104'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_104 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_105'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_105 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_106'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_106 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_107'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_107 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_108'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_108 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_109'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_109 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_110'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_110 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_111'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_111 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_112'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_112 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_113'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_113 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_114'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_114 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_115'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_115 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave__mutmut['xǁPostgresGazetteRepositoryǁsave__mutmut_116'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave__mutmut_116 # type: ignore # mutmut generated

mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['_mutmut_orig'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_orig # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_1'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_1 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_2'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_2 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_3'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_3 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_4'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_4 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_5'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_5 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_6'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_6 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_7'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_7 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_8'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_8 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_9'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_9 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_10'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_10 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_11'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_11 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_12'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_12 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_13'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_13 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_14'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_14 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_15'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_15 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_16'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_16 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_17'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_17 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_18'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_18 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_19'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_19 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_20'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_20 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_21'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_21 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_22'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_22 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_23'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_23 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_24'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_24 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_25'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_25 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_26'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_26 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_27'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_27 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_28'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_28 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_29'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_29 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_30'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_30 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_31'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_31 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_32'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_32 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_33'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_33 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_34'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_34 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_35'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_35 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_36'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_36 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_37'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_37 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_38'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_38 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_39'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_39 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_40'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_40 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_41'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_41 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_42'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_42 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_43'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_43 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_44'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_44 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_45'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_45 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut['xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_46'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_by_territory_and_date__mutmut_46 # type: ignore # mutmut generated

mutants_xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut['_mutmut_orig'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut_orig # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut['xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut_1'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut_1 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut['xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut_2'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut_2 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut['xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut_3'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut_3 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut['xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut_4'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut_4 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut['xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut_5'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut_5 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut['xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut_6'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut_6 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut['xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut_7'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁexists_by_hash__mutmut_7 # type: ignore # mutmut generated

mutants_xǁPostgresGazetteRepositoryǁsave_normative_act__mutmut['_mutmut_orig'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_act__mutmut_orig # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_act__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_act__mutmut_1'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_act__mutmut_1 # type: ignore # mutmut generated

mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['_mutmut_orig'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_orig # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_1'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_1 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_2'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_2 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_3'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_3 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_4'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_4 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_5'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_5 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_6'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_6 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_7'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_7 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_8'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_8 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_9'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_9 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_10'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_10 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_11'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_11 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_12'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_12 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_13'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_13 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_14'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_14 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_15'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_15 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_16'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_16 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_17'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_17 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_18'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_18 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_19'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_19 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_20'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_20 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_21'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_21 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_22'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_22 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_23'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_23 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_24'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_24 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_25'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_25 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_26'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_26 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_27'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_27 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_28'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_28 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_29'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_29 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_30'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_30 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_31'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_31 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_32'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_32 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_33'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_33 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_34'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_34 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_35'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_35 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_36'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_36 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_37'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_37 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_38'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_38 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_39'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_39 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_40'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_40 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_41'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_41 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_42'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_42 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_43'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_43 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_44'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_44 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_45'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_45 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_46'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_46 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_47'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_47 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_48'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_48 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_49'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_49 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_50'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_50 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_51'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_51 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_52'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_52 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_53'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_53 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_54'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_54 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_55'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_55 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_56'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_56 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_57'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_57 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_58'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_58 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_59'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_59 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_60'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_60 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_61'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_61 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_62'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_62 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_63'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_63 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_64'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_64 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_65'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_65 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_66'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_66 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_67'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_67 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_68'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_68 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_69'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_69 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_70'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_70 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_71'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_71 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_72'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_72 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_73'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_73 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_74'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_74 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_75'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_75 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_76'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_76 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_77'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_77 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_78'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_78 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_79'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_79 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_80'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_80 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_81'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_81 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_82'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_82 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_83'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_83 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_84'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_84 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_85'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_85 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_86'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_86 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_87'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_87 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_88'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_88 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_89'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_89 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_90'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_90 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_91'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_91 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_92'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_92 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_93'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_93 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_94'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_94 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_95'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_95 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_96'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_96 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_97'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_97 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_98'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_98 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_99'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_99 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_100'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_100 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_101'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_101 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_102'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_102 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_103'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_103 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_104'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_104 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_105'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_105 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_106'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_106 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_107'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_107 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_108'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_108 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_109'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_109 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_110'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_110 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_111'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_111 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_112'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_112 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_113'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_113 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_114'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_114 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_115'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_115 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_116'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_116 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_117'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_117 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_118'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_118 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_119'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_119 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_120'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_120 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_121'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_121 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_122'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_122 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_123'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_123 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_124'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_124 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_125'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_125 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_126'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_126 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_127'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_127 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_128'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_128 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_129'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_129 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_130'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_130 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_131'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_131 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_132'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_132 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_133'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_133 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_134'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_134 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_135'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_135 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_136'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_136 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_137'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_137 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_138'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_138 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_139'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_139 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_140'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_140 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_141'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_141 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_142'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_142 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_143'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_143 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_144'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_144 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_145'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_145 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_146'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_146 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_147'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_147 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_148'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_148 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_149'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_149 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_150'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_150 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_151'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_151 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_152'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_152 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_153'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_153 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_154'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_154 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_155'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_155 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_156'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_156 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_157'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_157 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_158'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_158 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_159'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_159 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_160'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_160 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_161'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_161 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_162'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_162 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_163'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_163 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_164'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_164 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_165'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_165 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_166'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_166 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_167'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_167 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_168'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_168 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_169'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_169 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_170'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_170 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_171'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_171 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_172'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_172 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_173'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_173 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_174'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_174 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_175'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_175 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_176'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_176 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_177'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_177 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_178'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_178 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_179'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_179 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_180'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_180 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_181'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_181 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_182'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_182 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_183'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_183 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_184'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_184 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_185'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_185 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_186'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_186 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_187'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_187 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_188'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_188 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_189'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_189 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_190'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_190 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_191'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_191 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_192'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_192 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_193'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_193 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_194'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_194 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut['xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_195'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁsave_normative_acts_bulk__mutmut_195 # type: ignore # mutmut generated

mutants_xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut['_mutmut_orig'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut_orig # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut['xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut_1'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut_1 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut['xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut_2'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut_2 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut['xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut_3'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut_3 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut['xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut_4'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut_4 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut['xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut_5'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut_5 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut['xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut_6'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut_6 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut['xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut_7'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁget_act_by_id__mutmut_7 # type: ignore # mutmut generated

mutants_xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut['_mutmut_orig'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut_orig # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut['xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut_1'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut_1 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut['xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut_2'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut_2 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut['xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut_3'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut_3 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut['xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut_4'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut_4 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut['xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut_5'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut_5 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut['xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut_6'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁfind_acts_by_edition__mutmut_6 # type: ignore # mutmut generated

mutants_xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut['_mutmut_orig'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut_orig # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut['xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut_1'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut_1 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut['xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut_2'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut_2 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut['xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut_3'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut_3 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut['xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut_4'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut_4 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut['xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut_5'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut_5 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut['xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut_6'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁexists_act_by_hash__mutmut_6 # type: ignore # mutmut generated

mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['_mutmut_orig'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_orig # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_1'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_1 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_2'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_2 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_3'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_3 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_4'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_4 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_5'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_5 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_6'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_6 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_7'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_7 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_8'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_8 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_9'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_9 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_10'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_10 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_11'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_11 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_12'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_12 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_13'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_13 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_14'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_14 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_15'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_15 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_16'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_16 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_17'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_17 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_18'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_18 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_19'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_19 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_20'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_20 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_21'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_21 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_22'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_22 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_23'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_23 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_24'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_24 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_25'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_25 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_26'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_26 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_27'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_27 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_28'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_28 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_29'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_29 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_30'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_30 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_31'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_31 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_32'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_32 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_33'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_33 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_34'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_34 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_35'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_35 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_36'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_36 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_37'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_37 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_38'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_38 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_39'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_39 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_40'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_40 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_41'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_41 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_42'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_42 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_43'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_43 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_44'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_44 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_45'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_45 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_46'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_46 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_47'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_47 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_48'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_48 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_49'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_49 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_50'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_50 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_51'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_51 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_52'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_52 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_53'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_53 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_54'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_54 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_55'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_55 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_56'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_56 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_57'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_57 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_58'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_58 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_59'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_59 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_60'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_60 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_61'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_61 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_62'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_62 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_63'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_63 # type: ignore # mutmut generated
mutants_xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut['xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_64'] = PostgresGazetteRepository.xǁPostgresGazetteRepositoryǁ_map_act_model_to_domain__mutmut_64 # type: ignore # mutmut generated
