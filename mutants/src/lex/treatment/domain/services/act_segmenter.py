"""LC 95/1998 Abstract Syntax Tree (AST) Segmenter for Legislative Acts.

Parses raw Brazilian legislative and normative text into a strictly validated
hierarchical AST (Articles -> Paragraphs -> Incisos -> Alíneas -> Items).
"""

import re
from uuid import UUID

from lex.treatment.domain.entities import ActAst, DispositivoNode
from lex.treatment.domain.value_objects import (
    CanonicalNodePath,
    DispositivoType,
)

# Hoisted compiled regex constants for provision labels
RE_ARTICLE = re.compile(
    r"^[Aa]rt\.\s*(\d+)(?:[ºo\.]|\.º)?(?:-([A-Za-z]{1,2}))?\.?\s*(.*)$",
    re.IGNORECASE,
)
RE_PARAGRAPH = re.compile(
    r"^(?:§\s*(\d+)(?:[ºo\.]|\.º)?(?:-([A-Za-z]{1,2}))?|([Pp]ar[áa]grafo\s+[úu]nico|PARÁGRAFO\s+ÚNICO))\.?\s*(.*)$",
    re.IGNORECASE,
)
RE_INCISO = re.compile(
    r"^([IVXLCDM]+)\s*[-–\.]\s*(.*)$",
)
RE_ALINEA = re.compile(
    r"^([a-z])\s*[\)\-]\s*(.*)$",
)
RE_ITEM = re.compile(
    r"^(\d+)\s*[\.\-]\s*(.*)$",
)

ROMAN_VALS: dict[str, int] = {
    "I": 1,
    "V": 5,
    "X": 10,
    "L": 50,
    "C": 100,
    "D": 500,
    "M": 1000,
}


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x__roman_to_int__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__roman_to_int__mutmut)
def _roman_to_int(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = roman_str.upper().strip()
    total = 0
    prev = 0
    for char in reversed(roman):
        val = ROMAN_VALS.get(char, 0)
        if val < prev:
            total -= val
        else:
            total += val
            prev = val
    return total if total > 0 else 1


def x__roman_to_int__mutmut_orig(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = roman_str.upper().strip()
    total = 0
    prev = 0
    for char in reversed(roman):
        val = ROMAN_VALS.get(char, 0)
        if val < prev:
            total -= val
        else:
            total += val
            prev = val
    return total if total > 0 else 1


def x__roman_to_int__mutmut_1(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = None
    total = 0
    prev = 0
    for char in reversed(roman):
        val = ROMAN_VALS.get(char, 0)
        if val < prev:
            total -= val
        else:
            total += val
            prev = val
    return total if total > 0 else 1


def x__roman_to_int__mutmut_2(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = roman_str.lower().strip()
    total = 0
    prev = 0
    for char in reversed(roman):
        val = ROMAN_VALS.get(char, 0)
        if val < prev:
            total -= val
        else:
            total += val
            prev = val
    return total if total > 0 else 1


def x__roman_to_int__mutmut_3(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = roman_str.upper().strip()
    total = None
    prev = 0
    for char in reversed(roman):
        val = ROMAN_VALS.get(char, 0)
        if val < prev:
            total -= val
        else:
            total += val
            prev = val
    return total if total > 0 else 1


def x__roman_to_int__mutmut_4(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = roman_str.upper().strip()
    total = 1
    prev = 0
    for char in reversed(roman):
        val = ROMAN_VALS.get(char, 0)
        if val < prev:
            total -= val
        else:
            total += val
            prev = val
    return total if total > 0 else 1


def x__roman_to_int__mutmut_5(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = roman_str.upper().strip()
    total = 0
    prev = None
    for char in reversed(roman):
        val = ROMAN_VALS.get(char, 0)
        if val < prev:
            total -= val
        else:
            total += val
            prev = val
    return total if total > 0 else 1


def x__roman_to_int__mutmut_6(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = roman_str.upper().strip()
    total = 0
    prev = 1
    for char in reversed(roman):
        val = ROMAN_VALS.get(char, 0)
        if val < prev:
            total -= val
        else:
            total += val
            prev = val
    return total if total > 0 else 1


def x__roman_to_int__mutmut_7(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = roman_str.upper().strip()
    total = 0
    prev = 0
    for char in reversed(None):
        val = ROMAN_VALS.get(char, 0)
        if val < prev:
            total -= val
        else:
            total += val
            prev = val
    return total if total > 0 else 1


def x__roman_to_int__mutmut_8(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = roman_str.upper().strip()
    total = 0
    prev = 0
    for char in reversed(roman):
        val = None
        if val < prev:
            total -= val
        else:
            total += val
            prev = val
    return total if total > 0 else 1


def x__roman_to_int__mutmut_9(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = roman_str.upper().strip()
    total = 0
    prev = 0
    for char in reversed(roman):
        val = ROMAN_VALS.get(None, 0)
        if val < prev:
            total -= val
        else:
            total += val
            prev = val
    return total if total > 0 else 1


def x__roman_to_int__mutmut_10(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = roman_str.upper().strip()
    total = 0
    prev = 0
    for char in reversed(roman):
        val = ROMAN_VALS.get(char, None)
        if val < prev:
            total -= val
        else:
            total += val
            prev = val
    return total if total > 0 else 1


def x__roman_to_int__mutmut_11(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = roman_str.upper().strip()
    total = 0
    prev = 0
    for char in reversed(roman):
        val = ROMAN_VALS.get(0)
        if val < prev:
            total -= val
        else:
            total += val
            prev = val
    return total if total > 0 else 1


def x__roman_to_int__mutmut_12(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = roman_str.upper().strip()
    total = 0
    prev = 0
    for char in reversed(roman):
        val = ROMAN_VALS.get(char, )
        if val < prev:
            total -= val
        else:
            total += val
            prev = val
    return total if total > 0 else 1


def x__roman_to_int__mutmut_13(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = roman_str.upper().strip()
    total = 0
    prev = 0
    for char in reversed(roman):
        val = ROMAN_VALS.get(char, 1)
        if val < prev:
            total -= val
        else:
            total += val
            prev = val
    return total if total > 0 else 1


def x__roman_to_int__mutmut_14(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = roman_str.upper().strip()
    total = 0
    prev = 0
    for char in reversed(roman):
        val = ROMAN_VALS.get(char, 0)
        if val <= prev:
            total -= val
        else:
            total += val
            prev = val
    return total if total > 0 else 1


def x__roman_to_int__mutmut_15(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = roman_str.upper().strip()
    total = 0
    prev = 0
    for char in reversed(roman):
        val = ROMAN_VALS.get(char, 0)
        if val < prev:
            total = val
        else:
            total += val
            prev = val
    return total if total > 0 else 1


def x__roman_to_int__mutmut_16(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = roman_str.upper().strip()
    total = 0
    prev = 0
    for char in reversed(roman):
        val = ROMAN_VALS.get(char, 0)
        if val < prev:
            total += val
        else:
            total += val
            prev = val
    return total if total > 0 else 1


def x__roman_to_int__mutmut_17(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = roman_str.upper().strip()
    total = 0
    prev = 0
    for char in reversed(roman):
        val = ROMAN_VALS.get(char, 0)
        if val < prev:
            total -= val
        else:
            total = val
            prev = val
    return total if total > 0 else 1


def x__roman_to_int__mutmut_18(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = roman_str.upper().strip()
    total = 0
    prev = 0
    for char in reversed(roman):
        val = ROMAN_VALS.get(char, 0)
        if val < prev:
            total -= val
        else:
            total -= val
            prev = val
    return total if total > 0 else 1


def x__roman_to_int__mutmut_19(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = roman_str.upper().strip()
    total = 0
    prev = 0
    for char in reversed(roman):
        val = ROMAN_VALS.get(char, 0)
        if val < prev:
            total -= val
        else:
            total += val
            prev = None
    return total if total > 0 else 1


def x__roman_to_int__mutmut_20(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = roman_str.upper().strip()
    total = 0
    prev = 0
    for char in reversed(roman):
        val = ROMAN_VALS.get(char, 0)
        if val < prev:
            total -= val
        else:
            total += val
            prev = val
    return total if total >= 0 else 1


def x__roman_to_int__mutmut_21(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = roman_str.upper().strip()
    total = 0
    prev = 0
    for char in reversed(roman):
        val = ROMAN_VALS.get(char, 0)
        if val < prev:
            total -= val
        else:
            total += val
            prev = val
    return total if total > 1 else 1


def x__roman_to_int__mutmut_22(roman_str: str) -> int:
    """Converts a Roman numeral string into an integer."""
    roman = roman_str.upper().strip()
    total = 0
    prev = 0
    for char in reversed(roman):
        val = ROMAN_VALS.get(char, 0)
        if val < prev:
            total -= val
        else:
            total += val
            prev = val
    return total if total > 0 else 2

mutants_x__roman_to_int__mutmut['_mutmut_orig'] = x__roman_to_int__mutmut_orig # type: ignore # mutmut generated
mutants_x__roman_to_int__mutmut['x__roman_to_int__mutmut_1'] = x__roman_to_int__mutmut_1 # type: ignore # mutmut generated
mutants_x__roman_to_int__mutmut['x__roman_to_int__mutmut_2'] = x__roman_to_int__mutmut_2 # type: ignore # mutmut generated
mutants_x__roman_to_int__mutmut['x__roman_to_int__mutmut_3'] = x__roman_to_int__mutmut_3 # type: ignore # mutmut generated
mutants_x__roman_to_int__mutmut['x__roman_to_int__mutmut_4'] = x__roman_to_int__mutmut_4 # type: ignore # mutmut generated
mutants_x__roman_to_int__mutmut['x__roman_to_int__mutmut_5'] = x__roman_to_int__mutmut_5 # type: ignore # mutmut generated
mutants_x__roman_to_int__mutmut['x__roman_to_int__mutmut_6'] = x__roman_to_int__mutmut_6 # type: ignore # mutmut generated
mutants_x__roman_to_int__mutmut['x__roman_to_int__mutmut_7'] = x__roman_to_int__mutmut_7 # type: ignore # mutmut generated
mutants_x__roman_to_int__mutmut['x__roman_to_int__mutmut_8'] = x__roman_to_int__mutmut_8 # type: ignore # mutmut generated
mutants_x__roman_to_int__mutmut['x__roman_to_int__mutmut_9'] = x__roman_to_int__mutmut_9 # type: ignore # mutmut generated
mutants_x__roman_to_int__mutmut['x__roman_to_int__mutmut_10'] = x__roman_to_int__mutmut_10 # type: ignore # mutmut generated
mutants_x__roman_to_int__mutmut['x__roman_to_int__mutmut_11'] = x__roman_to_int__mutmut_11 # type: ignore # mutmut generated
mutants_x__roman_to_int__mutmut['x__roman_to_int__mutmut_12'] = x__roman_to_int__mutmut_12 # type: ignore # mutmut generated
mutants_x__roman_to_int__mutmut['x__roman_to_int__mutmut_13'] = x__roman_to_int__mutmut_13 # type: ignore # mutmut generated
mutants_x__roman_to_int__mutmut['x__roman_to_int__mutmut_14'] = x__roman_to_int__mutmut_14 # type: ignore # mutmut generated
mutants_x__roman_to_int__mutmut['x__roman_to_int__mutmut_15'] = x__roman_to_int__mutmut_15 # type: ignore # mutmut generated
mutants_x__roman_to_int__mutmut['x__roman_to_int__mutmut_16'] = x__roman_to_int__mutmut_16 # type: ignore # mutmut generated
mutants_x__roman_to_int__mutmut['x__roman_to_int__mutmut_17'] = x__roman_to_int__mutmut_17 # type: ignore # mutmut generated
mutants_x__roman_to_int__mutmut['x__roman_to_int__mutmut_18'] = x__roman_to_int__mutmut_18 # type: ignore # mutmut generated
mutants_x__roman_to_int__mutmut['x__roman_to_int__mutmut_19'] = x__roman_to_int__mutmut_19 # type: ignore # mutmut generated
mutants_x__roman_to_int__mutmut['x__roman_to_int__mutmut_20'] = x__roman_to_int__mutmut_20 # type: ignore # mutmut generated
mutants_x__roman_to_int__mutmut['x__roman_to_int__mutmut_21'] = x__roman_to_int__mutmut_21 # type: ignore # mutmut generated
mutants_x__roman_to_int__mutmut['x__roman_to_int__mutmut_22'] = x__roman_to_int__mutmut_22 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut: MutantDict = {}  # type: ignore


class ActSegmenter:
    """Deterministic segmenter converting legislative text into an ActAst tree."""

    @classmethod
    @_mutmut_mutated(mutants_xǁActSegmenterǁsegment_text__mutmut, is_classmethod = True)
    def segment_text(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_orig(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_1(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = None
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_2(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = None

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_3(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = ""
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_4(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = ""
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_5(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = ""
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_6(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = ""
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_7(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = ""

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_8(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_9(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                break

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_10(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = None
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_11(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(None)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_12(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = None
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_13(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(None)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_14(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(2)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_15(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = None
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_16(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(None)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_17(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(3)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_18(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = None
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_19(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(None).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_20(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(4).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_21(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = None
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_22(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code = f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_23(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code -= f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_24(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.upper()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_25(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = None
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_26(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 or not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_27(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(None) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_28(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) < 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_29(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 10 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_30(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_31(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label = f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_32(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label -= f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_33(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.lower()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_34(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label = "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_35(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label -= "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_36(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "XX.XX"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_37(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = None
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_38(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=None,
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_39(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=None,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_40(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=None,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_41(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=None,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_42(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_43(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_44(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_45(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_46(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(None),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_47(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(None)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_48(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = None
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_49(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = ""
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_50(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = ""
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_51(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = ""
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_52(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = None
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_53(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                break

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_54(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is not None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_55(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                break

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_56(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = None
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_57(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(None)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_58(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = None
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_59(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(None)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_60(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(2)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_61(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = None
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_62(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(None)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_63(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(3)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_64(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = None
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_65(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(None)
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_66(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(None))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_67(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(4))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_68(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = None

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_69(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) and "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_70(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(None) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_71(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(5) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_72(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "XXXX").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_73(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico and not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_74(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_75(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = None
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_76(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = None
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_77(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "XXParágrafo único.XX"
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_78(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_79(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "PARÁGRAFO ÚNICO."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_80(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = None
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_81(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = None
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_82(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code = f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_83(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code -= f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_84(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.upper()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_85(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = None
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_86(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 or not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_87(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(None) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_88(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) < 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_89(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 10 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_90(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_91(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label = f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_92(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label -= f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_93(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.lower()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_94(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label = "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_95(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label -= "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_96(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "XX.XX"
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_97(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = None

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_98(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = None
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_99(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=None,
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_100(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=None,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_101(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=None,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_102(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=None,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_103(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_104(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_105(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_106(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_107(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(None),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_108(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(None)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_109(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_110(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = ""
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_111(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = ""
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_112(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = None
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_113(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                break

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_114(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = None
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_115(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(None)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_116(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = None
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_117(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).lower()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_118(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(None).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_119(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(2).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_120(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = None
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_121(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(None).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_122(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(3).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_123(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = None

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_124(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(None)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_125(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = None
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_126(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_127(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = None
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_128(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = None

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_129(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = None
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_130(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=None,
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_131(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=None,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_132(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=None,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_133(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=None,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_134(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_135(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_136(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_137(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_138(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(None),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_139(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(None)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_140(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_141(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = ""
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_142(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = None
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_143(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                break

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_144(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = None
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_145(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(None)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_146(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali or (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_147(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None and current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_148(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_149(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_150(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = None
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_151(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).upper()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_152(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(None).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_153(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(2).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_154(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = None

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_155(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(None).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_156(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(3).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_157(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = None
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_158(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_159(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is not None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_160(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    break
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_161(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = None
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_162(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = None

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_163(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = None
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_164(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=None,
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_165(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=None,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_166(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=None,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_167(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=None,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_168(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_169(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_170(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_171(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_172(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(None),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_173(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(None)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_174(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = None
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_175(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = None
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_176(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                break

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_177(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = None
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_178(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(None)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_179(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item or current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_180(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_181(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = None
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_182(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(None)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_183(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(2)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_184(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = None

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_185(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(None).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_186(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(3).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_187(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = None
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_188(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = None

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_189(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = None
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_190(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=None,
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_191(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=None,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_192(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=None,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_193(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=None,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_194(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_195(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_196(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_197(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_198(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(None),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_199(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(None)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_200(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = None
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_201(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                break

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_202(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_203(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = None

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_204(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=None,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_205(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=None,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_206(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=None,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_207(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=None,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_208(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=None,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_209(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_210(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            title=title,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_211(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            ementa=ementa,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_212(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            nodes=root_nodes,
        )

    @classmethod
    def xǁActSegmenterǁsegment_text__mutmut_213(
        cls,
        raw_text: str,
        title: str,
        ementa: str | None = None,
        act_id: UUID | None = None,
        canonical_urn: str | None = None,
    ) -> ActAst:
        """Parses raw text into an ActAst aggregate root.

        Args:
            raw_text: Plain text of the legislative act.
            title: Title of the act.
            ementa: Optional summary / ementa.
            act_id: Optional UUID of the parent normative act.
            canonical_urn: Optional canonical LexML URN.

        Returns:
            A structured ActAst tree.
        """
        lines = [line.strip() for line in raw_text.splitlines()]
        root_nodes: list[DispositivoNode] = []

        current_art: DispositivoNode | None = None
        current_par: DispositivoNode | None = None
        current_inc: DispositivoNode | None = None
        current_ali: DispositivoNode | None = None
        current_active: DispositivoNode | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check Article
            m_art = RE_ARTICLE.match(line)
            if m_art:
                num = m_art.group(1)
                suffix = m_art.group(2)
                rest = m_art.group(3).strip()
                art_code = f"art_{num}"
                if suffix:
                    art_code += f"_{suffix.lower()}"

                label = f"Art. {num}º" if int(num) <= 9 and not suffix else f"Art. {num}"
                if suffix:
                    label += f"-{suffix.upper()}"
                label += "."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(art_code),
                    node_type=DispositivoType.ARTIGO,
                    label=label,
                    text=rest,
                )
                root_nodes.append(node)
                current_art = node
                current_par = None
                current_inc = None
                current_ali = None
                current_active = node
                continue

            if current_art is None:
                # Header lines before the first article (e.g. preambles, formulas)
                continue

            # 2. Check Paragraph
            m_par = RE_PARAGRAPH.match(line)
            if m_par:
                num = m_par.group(1)
                par_suffix = m_par.group(2)
                is_unico = bool(m_par.group(3))
                rest = (m_par.group(4) or "").strip()

                if is_unico or not num:
                    par_code = f"{current_art.node_path.value}.par_unico"
                    label = "Parágrafo único."
                    node_type = DispositivoType.PARAGRAFO_UNICO
                else:
                    par_code = f"{current_art.node_path.value}.par_{num}"
                    if par_suffix:
                        par_code += f"_{par_suffix.lower()}"
                    label = f"§ {num}º" if int(num) <= 9 and not par_suffix else f"§ {num}"
                    if par_suffix:
                        label += f"-{par_suffix.upper()}"
                    label += "."
                    node_type = DispositivoType.PARAGRAFO

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(par_code),
                    node_type=node_type,
                    label=label,
                    text=rest,
                )
                current_art.add_child(node)
                current_par = node
                current_inc = None
                current_ali = None
                current_active = node
                continue

            # 3. Check Inciso (Clause)
            m_inc = RE_INCISO.match(line)
            if m_inc:
                roman = m_inc.group(1).upper()
                rest = m_inc.group(2).strip()
                inc_num = _roman_to_int(roman)

                parent = current_par if current_par is not None else current_art
                inc_code = f"{parent.node_path.value}.inc_{inc_num}"
                label = f"{roman} -"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(inc_code),
                    node_type=DispositivoType.INCISO,
                    label=label,
                    text=rest,
                )
                parent.add_child(node)
                current_inc = node
                current_ali = None
                current_active = node
                continue

            # 4. Check Alínea
            m_ali = RE_ALINEA.match(line)
            if m_ali and (current_inc is not None or current_par is not None):
                letter = m_ali.group(1).lower()
                rest = m_ali.group(2).strip()

                parent_node = current_inc if current_inc is not None else current_par
                if parent_node is None:
                    continue
                ali_code = f"{parent_node.node_path.value}.ali_{letter}"
                label = f"{letter})"

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(ali_code),
                    node_type=DispositivoType.ALINEA,
                    label=label,
                    text=rest,
                )
                parent_node.add_child(node)
                current_ali = node
                current_active = node
                continue

            # 5. Check Item
            m_item = RE_ITEM.match(line)
            if m_item and current_ali is not None:
                item_num = m_item.group(1)
                rest = m_item.group(2).strip()

                item_code = f"{current_ali.node_path.value}.item_{item_num}"
                label = f"{item_num}."

                node = DispositivoNode(
                    node_path=CanonicalNodePath.from_string(item_code),
                    node_type=DispositivoType.ITEM,
                    label=label,
                    text=rest,
                )
                current_ali.add_child(node)
                current_active = node
                continue

            # 6. Continuation line for the most specific active provision
            if current_active is not None:
                current_active.text = (
                    f"{current_active.text} {line}".strip() if current_active.text else line
                )

        return ActAst(
            act_id=act_id,
            canonical_urn=canonical_urn,
            title=title,
            ementa=ementa,
            )

mutants_xǁActSegmenterǁsegment_text__mutmut['_mutmut_orig'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_orig # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_1'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_1 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_2'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_2 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_3'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_3 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_4'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_4 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_5'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_5 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_6'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_6 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_7'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_7 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_8'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_8 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_9'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_9 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_10'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_10 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_11'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_11 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_12'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_12 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_13'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_13 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_14'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_14 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_15'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_15 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_16'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_16 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_17'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_17 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_18'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_18 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_19'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_19 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_20'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_20 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_21'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_21 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_22'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_22 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_23'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_23 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_24'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_24 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_25'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_25 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_26'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_26 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_27'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_27 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_28'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_28 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_29'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_29 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_30'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_30 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_31'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_31 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_32'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_32 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_33'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_33 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_34'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_34 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_35'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_35 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_36'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_36 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_37'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_37 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_38'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_38 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_39'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_39 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_40'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_40 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_41'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_41 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_42'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_42 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_43'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_43 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_44'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_44 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_45'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_45 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_46'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_46 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_47'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_47 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_48'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_48 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_49'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_49 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_50'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_50 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_51'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_51 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_52'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_52 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_53'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_53 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_54'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_54 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_55'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_55 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_56'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_56 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_57'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_57 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_58'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_58 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_59'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_59 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_60'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_60 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_61'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_61 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_62'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_62 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_63'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_63 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_64'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_64 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_65'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_65 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_66'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_66 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_67'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_67 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_68'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_68 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_69'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_69 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_70'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_70 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_71'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_71 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_72'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_72 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_73'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_73 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_74'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_74 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_75'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_75 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_76'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_76 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_77'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_77 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_78'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_78 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_79'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_79 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_80'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_80 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_81'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_81 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_82'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_82 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_83'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_83 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_84'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_84 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_85'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_85 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_86'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_86 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_87'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_87 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_88'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_88 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_89'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_89 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_90'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_90 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_91'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_91 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_92'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_92 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_93'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_93 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_94'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_94 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_95'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_95 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_96'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_96 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_97'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_97 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_98'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_98 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_99'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_99 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_100'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_100 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_101'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_101 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_102'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_102 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_103'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_103 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_104'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_104 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_105'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_105 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_106'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_106 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_107'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_107 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_108'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_108 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_109'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_109 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_110'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_110 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_111'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_111 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_112'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_112 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_113'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_113 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_114'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_114 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_115'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_115 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_116'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_116 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_117'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_117 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_118'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_118 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_119'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_119 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_120'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_120 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_121'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_121 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_122'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_122 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_123'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_123 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_124'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_124 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_125'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_125 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_126'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_126 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_127'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_127 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_128'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_128 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_129'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_129 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_130'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_130 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_131'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_131 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_132'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_132 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_133'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_133 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_134'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_134 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_135'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_135 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_136'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_136 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_137'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_137 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_138'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_138 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_139'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_139 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_140'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_140 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_141'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_141 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_142'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_142 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_143'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_143 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_144'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_144 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_145'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_145 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_146'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_146 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_147'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_147 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_148'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_148 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_149'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_149 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_150'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_150 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_151'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_151 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_152'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_152 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_153'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_153 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_154'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_154 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_155'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_155 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_156'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_156 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_157'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_157 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_158'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_158 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_159'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_159 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_160'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_160 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_161'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_161 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_162'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_162 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_163'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_163 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_164'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_164 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_165'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_165 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_166'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_166 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_167'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_167 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_168'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_168 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_169'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_169 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_170'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_170 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_171'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_171 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_172'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_172 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_173'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_173 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_174'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_174 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_175'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_175 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_176'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_176 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_177'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_177 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_178'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_178 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_179'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_179 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_180'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_180 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_181'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_181 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_182'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_182 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_183'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_183 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_184'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_184 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_185'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_185 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_186'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_186 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_187'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_187 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_188'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_188 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_189'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_189 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_190'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_190 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_191'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_191 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_192'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_192 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_193'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_193 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_194'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_194 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_195'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_195 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_196'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_196 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_197'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_197 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_198'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_198 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_199'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_199 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_200'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_200 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_201'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_201 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_202'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_202 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_203'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_203 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_204'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_204 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_205'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_205 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_206'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_206 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_207'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_207 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_208'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_208 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_209'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_209 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_210'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_210 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_211'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_211 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_212'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_212 # type: ignore # mutmut generated
mutants_xǁActSegmenterǁsegment_text__mutmut['xǁActSegmenterǁsegment_text__mutmut_213'] = ActSegmenter.xǁActSegmenterǁsegment_text__mutmut_213 # type: ignore # mutmut generated
