"""LC 95/1998 Mutation Extractor for Legislative Amendments.

Extracts atomic legislative patch deltas (ALTERACAO_NR, REVOGACAO_EXPRESSA, ACRESCIMO)
from amending statutes and prepares them for the CQRS Mutation Ledger.
"""

import re
import uuid
from uuid import UUID

from lex.ingestion.domain.value_objects import DocumentHash, GazetteDate
from lex.treatment.domain.entities import NormativeActMutation
from lex.treatment.domain.services.act_segmenter import _roman_to_int
from lex.treatment.domain.value_objects import (
    CanonicalNodePath,
    MutationType,
)

# Hoisted compiled regex constants
RE_ALTERATION_HEADER = re.compile(
    r"[Aa]rt\.\s*\d+[ºo]?\s+(?:O|A|Os|As)?\s*(Lei|Decreto|Medida\s+Provisória|Portaria|Resolução)"
    r"(?:\s+Complementar)?\s+(?:n[ºo°\.]?\s*)?([\d\.]+)"
    r"(?:[,\s/]+(?:de\s+)?(?:[\w\s]+(?:de\s+)?)?(\d{4}))?,?\s+passa[m]?\s+a\s+vigorar\s+com\s+a[s]?\s+seguinte[s]?\s+alteraç[ãõ]e[s]?:?",
    re.IGNORECASE,
)

RE_QUOTED_ARTICLE = re.compile(
    r'["“\']?\s*[Aa]rt\.\s*(\d+)(?:[ºo\.]|\.º)?(?:-([A-Za-z]{1,2}))?\.?\s*(.*)$',
    re.IGNORECASE,
)

RE_QUOTED_PARAGRAPH = re.compile(
    r'["“\']?\s*(?:§\s*(\d+)(?:[ºo\.]|\.º)?(?:-([A-Za-z]{1,2}))?|([Pp]ar[áa]grafo\s+[úu]nico|PARÁGRAFO\s+ÚNICO))\.?\s*(.*)$',
    re.IGNORECASE,
)

RE_QUOTED_INCISO = re.compile(
    r'["“\']?\s*([IVXLCDM]+)\s*[-–\.]\s*(.*)$',
)

RE_NR_MARKER = re.compile(
    r"\s*\(NR\)\s*[\"”\']?$",
    re.IGNORECASE,
)

RE_EXPRESS_REVOCATION_LINE = re.compile(
    r"[Rr]evoga[m]?\s*-\s*se\s+(?:expressamente\s+)?(.+)",
    re.IGNORECASE,
)

RE_REVOKE_ART = re.compile(
    r"[oa]s?\s+art(?:igo)?s?\.\s*(\d+)(?:[ºo\.]|\.º)?(?:-([A-Za-z]{1,2}))?",
    re.IGNORECASE,
)

RE_REVOKE_INCISOS = re.compile(
    r"[oa]s?\s+incisos?\s+([IVXLCDM\s,e–\-]+)\s+do\s+(?:caput\s+do\s+)?art(?:igo)?\.\s*(\d+)",
    re.IGNORECASE,
)


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁMutationExtractorǁextract_mutations__mutmut: MutantDict = {}  # type: ignore


class MutationExtractor:
    """Deterministic extractor for statutory amendments and express revocations."""

    @classmethod
    @_mutmut_mutated(mutants_xǁMutationExtractorǁextract_mutations__mutmut, is_classmethod = True)
    def extract_mutations(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_orig(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_1(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "XXBRXX",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_2(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "br",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_3(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = None
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_4(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id and uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_5(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            None, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_6(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, None
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_7(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_8(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_9(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = None
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_10(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = None

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_11(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = None
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_12(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = True
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_13(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = ""
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_14(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = ""

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_15(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_16(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                break

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_17(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() and "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_18(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "XXrevoga-seXX" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_19(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "REVOGA-SE" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_20(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" not in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_21(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.upper() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_22(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "XXrevogam-seXX" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_23(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "REVOGAM-SE" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_24(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" not in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_25(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.upper():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_26(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = None
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_27(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(None)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_28(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = None
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_29(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(None)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_30(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(2)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_31(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = None
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_32(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(None)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_33(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(3)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_34(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = None
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_35(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(None, incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_36(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", None)
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_37(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_38(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", )
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_39(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"XX\b[IVXLCDM]+\bXX", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_40(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[ivxlcdm]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_41(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.lower())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_42(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = None
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_43(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(None)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_44(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = None
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_45(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = None
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_46(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            None
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_47(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            None
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_48(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=None,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_49(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=None,
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_50(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=None,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_51(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=None,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_52(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=None,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_53(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=None,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_54(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source=None,
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_55(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=None,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_56(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=None,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_57(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_58(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_59(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_60(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_61(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_62(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_63(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_64(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_65(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_66(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_67(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(None),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_68(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="XXlc95_deterministic_regexXX",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_69(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="LC95_DETERMINISTIC_REGEX",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_70(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=2.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_71(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    break

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_72(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = None
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_73(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(None)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_74(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = None
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_75(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(None)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_76(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(2)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_77(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = None
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_78(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(None)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_79(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(3)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_80(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = None
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_81(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code = f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_82(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code -= f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_83(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.upper()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_84(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = None
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_85(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        None
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_86(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        None
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_87(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=None,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_88(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=None,
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_89(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=None,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_90(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=None,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_91(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=None,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_92(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=None,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_93(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source=None,
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_94(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=None,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_95(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=None,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_96(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_97(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_98(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_99(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_100(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_101(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_102(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_103(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_104(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_105(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_106(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(None),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_107(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="XXlc95_deterministic_regexXX",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_108(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="LC95_DETERMINISTIC_REGEX",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_109(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=2.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_110(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    break

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_111(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(None):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_112(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = None
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_113(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = False
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_114(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = ""
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_115(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = ""
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_116(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                break

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_117(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = None
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_118(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(None)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_119(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = None
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_120(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(None)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_121(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(2)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_122(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = None
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_123(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(None)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_124(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(3)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_125(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = None
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_126(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code = f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_127(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code -= f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_128(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.upper()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_129(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = ""
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_130(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = None
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_131(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(None).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_132(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(4).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_133(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(None):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_134(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = None
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_135(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(None)
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_136(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            None
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_137(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=None,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_138(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=None,
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_139(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=None,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_140(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=None,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_141(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_142(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=None,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_143(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=None,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_144(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source=None,
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_145(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=None,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_146(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=None,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_147(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_148(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_149(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_150(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_151(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_152(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_153(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_154(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_155(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_156(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_157(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(None),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_158(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="XXlc95_deterministic_regexXX",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_159(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="LC95_DETERMINISTIC_REGEX",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_160(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=2.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_161(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    break

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_162(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = None
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_163(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(None)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_164(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par or current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_165(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_166(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = None
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_167(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(None)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_168(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(2)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_169(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = None
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_170(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(None)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_171(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(3)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_172(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = None
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_173(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(None)
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_174(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(None))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_175(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(4))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_176(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = None

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_177(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) and "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_178(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(None) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_179(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(5) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_180(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "XXXX").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_181(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico and not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_182(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_183(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = None
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_184(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = None
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_185(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code = f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_186(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code -= f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_187(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.upper()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_188(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(None):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_189(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = None
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_190(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(None)
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_191(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            None
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_192(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=None,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_193(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=None,
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_194(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=None,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_195(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=None,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_196(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_197(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=None,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_198(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=None,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_199(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source=None,
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_200(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=None,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_201(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=None,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_202(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_203(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_204(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_205(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_206(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_207(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_208(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_209(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_210(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_211(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_212(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(None),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_213(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="XXlc95_deterministic_regexXX",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_214(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="LC95_DETERMINISTIC_REGEX",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_215(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=2.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_216(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    break

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_217(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = None
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_218(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(None)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_219(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc or current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_220(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_221(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = None
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_222(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).lower()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_223(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(None).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_224(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(2).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_225(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = None
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_226(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(None).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_227(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(3).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_228(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = None

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_229(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(None)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_230(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = None
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_231(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_232(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = None

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_233(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(None):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_234(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = None
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_235(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(None)
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_236(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            None
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_237(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=None,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_238(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=None,
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_239(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=None,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_240(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=None,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_241(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_242(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=None,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_243(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=None,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_244(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source=None,
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_245(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=None,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_246(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=None,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_247(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_248(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_249(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_250(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_251(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_252(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_253(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_254(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_255(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_256(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_257(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(None),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_258(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="XXlc95_deterministic_regexXX",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_259(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="LC95_DETERMINISTIC_REGEX",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_260(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=2.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

        return mutations

    @classmethod
    def xǁMutationExtractorǁextract_mutations__mutmut_261(
        cls,
        raw_text: str,
        author_act_id: UUID,
        publication_date: GazetteDate,
        effective_date: GazetteDate,
        target_act_id: UUID | None = None,
        default_territory_id: str = "BR",
    ) -> list[NormativeActMutation]:
        """Parses amending text and returns all extracted NormativeActMutation items.

        Args:
            raw_text: Full text of the amending statute.
            author_act_id: UUID of the amending act.
            publication_date: Publication date of the amending act.
            effective_date: Effective date of the amending act.
            target_act_id: Optional UUID of target act if already resolved.
            default_territory_id: Territory code fallback.

        Returns:
            List of validated NormativeActMutation entities.
        """
        resolved_target_id = target_act_id or uuid.uuid5(
            uuid.NAMESPACE_DNS, f"{default_territory_id}:target_placeholder"
        )
        mutations: list[NormativeActMutation] = []
        lines = [line.strip() for line in raw_text.splitlines()]

        in_alteration_block = False
        current_art_code: str | None = None
        current_par_code: str | None = None

        for line in lines:
            if not line:
                continue

            # 1. Check for express revocations
            if "revoga-se" in line.lower() or "revogam-se" in line.lower():
                m_rev_inc = RE_REVOKE_INCISOS.search(line)
                if m_rev_inc:
                    incisos_str = m_rev_inc.group(1)
                    art_num = m_rev_inc.group(2)
                    # Extract Roman numerals
                    romans = re.findall(r"\b[IVXLCDM]+\b", incisos_str.upper())
                    for roman in romans:
                        inc_num = _roman_to_int(roman)
                        node_path_str = f"art_{art_num}.inc_{inc_num}"
                        mut_hash = DocumentHash.from_text(
                            f"REVOGACAO_EXPRESSA:{node_path_str}:{author_act_id}"
                        )
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(node_path_str),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.REVOGACAO_EXPRESSA,
                                new_text=None,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                m_rev_art = RE_REVOKE_ART.search(line)
                if m_rev_art:
                    art_num = m_rev_art.group(1)
                    suffix = m_rev_art.group(2)
                    art_code = f"art_{art_num}"
                    if suffix:
                        art_code += f"_{suffix.lower()}"
                    mut_hash = DocumentHash.from_text(
                        f"REVOGACAO_EXPRESSA:{art_code}:{author_act_id}"
                    )
                    mutations.append(
                        NormativeActMutation(
                            target_act_id=resolved_target_id,
                            target_node_path=CanonicalNodePath.from_string(art_code),
                            author_act_id=author_act_id,
                            mutation_type=MutationType.REVOGACAO_EXPRESSA,
                            new_text=None,
                            publication_date=publication_date,
                            effective_date=effective_date,
                            extraction_source="lc95_deterministic_regex",
                            confidence_score=1.0,
                            mutation_sha256=mut_hash,
                        )
                    )
                    continue

            # 2. Check for alteration header
            if RE_ALTERATION_HEADER.search(line):
                in_alteration_block = True
                current_art_code = None
                current_par_code = None
                continue

            if in_alteration_block:
                # Check for article anchor in quoted section
                m_art = RE_QUOTED_ARTICLE.match(line)
                if m_art:
                    num = m_art.group(1)
                    suffix = m_art.group(2)
                    current_art_code = f"art_{num}"
                    if suffix:
                        current_art_code += f"_{suffix.lower()}"
                    current_par_code = None
                    rest = m_art.group(3).strip()
                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_art_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_art_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for paragraph in quoted section
                m_par = RE_QUOTED_PARAGRAPH.match(line)
                if m_par and current_art_code is not None:
                    num = m_par.group(1)
                    par_suffix = m_par.group(2)
                    is_unico = bool(m_par.group(3))
                    rest = (m_par.group(4) or "").strip()

                    if is_unico or not num:
                        current_par_code = f"{current_art_code}.par_unico"
                    else:
                        current_par_code = f"{current_art_code}.par_{num}"
                        if par_suffix:
                            current_par_code += f"_{par_suffix.lower()}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{current_par_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(current_par_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    continue

                # Check for inciso in quoted section
                m_inc = RE_QUOTED_INCISO.match(line)
                if m_inc and current_art_code is not None:
                    roman = m_inc.group(1).upper()
                    rest = m_inc.group(2).strip()
                    inc_num = _roman_to_int(roman)

                    parent = current_par_code if current_par_code is not None else current_art_code
                    inc_code = f"{parent}.inc_{inc_num}"

                    if RE_NR_MARKER.search(rest):
                        mut_hash = DocumentHash.from_text(f"ALTERACAO_NR:{inc_code}:{rest}")
                        mutations.append(
                            NormativeActMutation(
                                target_act_id=resolved_target_id,
                                target_node_path=CanonicalNodePath.from_string(inc_code),
                                author_act_id=author_act_id,
                                mutation_type=MutationType.ALTERACAO_NR,
                                new_text=rest,
                                publication_date=publication_date,
                                effective_date=effective_date,
                                extraction_source="lc95_deterministic_regex",
                                confidence_score=1.0,
                                mutation_sha256=mut_hash,
                            )
                        )
                    break

        return mutations

mutants_xǁMutationExtractorǁextract_mutations__mutmut['_mutmut_orig'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_orig # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_1'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_1 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_2'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_2 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_3'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_3 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_4'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_4 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_5'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_5 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_6'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_6 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_7'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_7 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_8'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_8 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_9'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_9 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_10'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_10 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_11'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_11 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_12'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_12 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_13'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_13 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_14'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_14 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_15'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_15 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_16'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_16 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_17'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_17 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_18'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_18 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_19'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_19 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_20'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_20 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_21'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_21 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_22'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_22 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_23'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_23 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_24'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_24 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_25'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_25 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_26'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_26 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_27'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_27 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_28'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_28 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_29'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_29 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_30'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_30 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_31'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_31 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_32'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_32 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_33'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_33 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_34'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_34 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_35'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_35 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_36'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_36 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_37'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_37 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_38'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_38 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_39'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_39 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_40'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_40 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_41'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_41 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_42'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_42 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_43'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_43 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_44'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_44 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_45'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_45 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_46'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_46 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_47'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_47 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_48'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_48 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_49'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_49 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_50'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_50 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_51'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_51 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_52'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_52 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_53'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_53 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_54'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_54 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_55'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_55 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_56'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_56 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_57'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_57 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_58'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_58 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_59'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_59 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_60'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_60 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_61'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_61 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_62'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_62 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_63'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_63 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_64'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_64 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_65'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_65 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_66'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_66 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_67'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_67 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_68'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_68 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_69'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_69 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_70'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_70 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_71'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_71 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_72'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_72 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_73'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_73 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_74'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_74 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_75'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_75 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_76'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_76 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_77'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_77 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_78'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_78 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_79'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_79 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_80'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut_80 # type: ignore # mutmut generated
mutants_xǁMutationExtractorǁextract_mutations__mutmut['xǁMutationExtractorǁextract_mutations__mutmut_81'] = MutationExtractor.xǁMutationExtractorǁextract_mutations__mutmut